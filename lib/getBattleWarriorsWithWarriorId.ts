/* import db from "../db";

function getBattleWarriorsWithWarriorId(warriorId): Promise<any> {
  try {
    return new Promise((resolve, reject) => {
      db.query(
      );
    });
  } catch (error) {
    console.error(error);
  }
}

export default getBattleWarriorsWithWarriorId; */
